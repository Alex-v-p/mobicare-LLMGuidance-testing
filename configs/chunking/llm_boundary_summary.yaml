strategy: llm_boundary_summary
# Same boundaries as llm_boundary.yaml; the runner will also evaluate
# a summary-augmented variant.
max_tokens: 450
min_tokens: 80
boundary_window: 6
max_retries: 2
retry_backoff_s: 0.8
